# Spring Reactive REST Service

## Endpoints
1. `/`
2. `/low`
3. `/lower-middle`
4. `/upper-middle`
5. `/high-oecd`
6. `/high-non-oecd`

## JSON
```
% curl http://localhost:8083
% curl -H "Accept: application/json" http://localhost:8083/low
```
